// Import necessary classes and libraries
package com.ppk.locations;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

// Declare the main application class and annotate it with @SpringBootApplication
@SpringBootApplication
public class LocationsApplication {

	// Define the main method that serves as the application's entry point
	public static void main(String[] args) {
		// Launch the Spring Boot application using the LocationsApplication class and the provided arguments
		SpringApplication.run(LocationsApplication.class, args);
	}
}
